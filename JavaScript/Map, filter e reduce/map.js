//iterando pelo array usando MAP
const arrayMap = [1, 2, 3, 4, 5];
console.log(arrayMap.map((item) => item * 2)); //output [2, 4, 6, 8, 10]

//iterando pelo array usando forEach

const arrayForEach = [1, 2, 3, 4, 5];
console.log(arrayForEach.forEach((item) => item * 2)); //output undefined

